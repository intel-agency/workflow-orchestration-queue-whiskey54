# AI Repository Summary

> Auto-generated summary for AI agents and developers

## Repository Overview

**Name**: workflow-orchestration-queue-whiskey54  
**Type**: GitHub Template Repository  
**Purpose**: GitHub Actions-based AI orchestration system with FastAPI queue service

## Architecture

### Core Components

1. **Orchestration System** (existing template infrastructure)
   - `.github/workflows/orchestrator-agent.yml` - Primary workflow
   - `.opencode/` - Agent definitions and commands
   - DevContainer infrastructure for reproducible environments

2. **Application Service** (new Python project)
   - `src/notifier_service.py` - FastAPI HTTP service
   - `src/orchestrator_sentinel.py` - Background monitoring
   - `src/models/` - Pydantic data models
   - `src/queue/` - Queue management

### Technology Stack

| Layer | Technology |
|-------|------------|
| Runtime | Python 3.12+ |
| Web Framework | FastAPI |
| Validation | Pydantic v2 |
| HTTP Client | HTTPX |
| Package Manager | uv |
| Containerization | Docker |
| CI/CD | GitHub Actions |

## Key Files

### Configuration
- `pyproject.toml` - Project metadata, dependencies, tool configs
- `.python-version` - Python version pin (3.12)
- `Dockerfile` - Multi-stage Python container
- `docker-compose.yml` - Local development orchestration

### Source Code
- `src/notifier_service.py` - FastAPI app with `/health` and `/ready` endpoints
- `src/orchestrator_sentinel.py` - Async background service
- `src/models/work_item.py` - Work item state machine (pending→processing→completed/failed)
- `src/models/github_events.py` - GitHub webhook payload models
- `src/queue/github_queue.py` - In-memory queue with priority and retry

### Tests
- `tests/conftest.py` - Pytest fixtures
- `tests/test_work_item.py` - Model tests
- `tests/test_queue.py` - Queue operation tests

## Development Workflow

### Setup
```bash
uv sync  # Install dependencies
```

### Quality Checks
```bash
uv run ruff check .     # Lint
uv run ruff format .    # Format
uv run mypy src/        # Type check
uv run pytest           # Test
```

### Docker
```bash
docker compose up -d    # Start services
docker compose logs -f  # View logs
```

## Important Constraints

1. **Docker Healthchecks**: Must use Python stdlib (`urllib`), not `curl`
2. **GitHub Actions**: All actions must be pinned by full SHA (not tags)
3. **No Secrets**: Never commit API keys or tokens
4. **Scaffolding Only**: This is project structure, not full implementation

## CI/CD Notes

- Existing workflows in `.github/workflows/` are already SHA-pinned
- `validate.yml` runs lint, scan, and test jobs
- `orchestrator-agent.yml` handles AI orchestration triggers

## Memory Context

This repository uses a knowledge graph memory system stored in `.memory/`. Agents should:
- Read memory at task start (`read_graph`/`search_nodes`)
- Persist findings after significant work (`create_entities`/`add_observations`)

---
*Generated: 2026-03-27*
